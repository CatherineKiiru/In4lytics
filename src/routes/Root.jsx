import React from "react";
import NavBar from "../common/NavBar";
import { Outlet } from "react-router-dom";

const Root = () => {
  return (
    <div>
      <NavBar />
    </div>
  );
};

export default Root;
